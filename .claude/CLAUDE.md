# Project-Specific Instructions

## Git Workflow

- Always use squash merging when merging PRs: `gh pr merge --squash`
- Do not ask for confirmation when running `gh` commands — execute them directly
